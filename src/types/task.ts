export type Task = {
    id:string
    title:string
    completed:boolean 
    createdAt:number
}

/*Isso significa:

* `id` → sempre string
* `title` → sempre texto
* `completed` → sempre boolean
* `createdAt` → sempre número (timestamp)*/